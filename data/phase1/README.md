How to run?
- `feature_extraction.py` is the file with all functions we need to extract the feature. 
- `run_phase1.py` is the python file that run all the functions. 
- Before running, make sure the `samples.csv` in the same file as `run_phase1.py`.
- In a new terminal, `python run_phase1.py --input samples.csv`
